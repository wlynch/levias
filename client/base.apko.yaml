contents:
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  repositories:
    - https://packages.wolfi.dev/os
  packages:
    - ca-certificates-bundle
    - wolfi-base
    - bash
    - docker-cli
    - go
    - melange

entrypoint:
  command: /bin/bash

archs:
  - x86_64
